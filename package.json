{
  "type": "module",
  "name": "promotion-factory",
  "version": "1.0.0",
  "description": "Sitio web de Promotion Factory",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup --config --watch",
    "sass": "sass --watch --style=expanded  src/custom.scss css/bundle.css"
  },
  "author": "Huacal Design",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^4.13.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "jquery-validation": "^1.20.0"
  }
}
